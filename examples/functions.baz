fn method1(arg1: float, arg2: str): int {
	if (arg1 < 5.0) {
		return 5;
	} else {
		return 6;
	}
}

fn method2(): void {}

fn main(): void {
	method2();

	let string_val: str = "hello world";
	let asdf: int = method1(2.5, string_val);
	println(asdf);
}
